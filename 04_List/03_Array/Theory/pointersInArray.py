def main():
    arr = [1, 2, 3, 4, 5]
    # Python lists don't use pointers in the same way as C++
    # But we can iterate through the list
    
    # Printing elements
    for element in arr:
        print(element, end=" ")

if __name__ == "__main__":
    main()
